import 'package:flutter/material.dart';

class BidScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Bid"),
      ),
      body: Center(
        child: Text("Bid Screen"),
      ),
    );
  }
}
