import 'package:flutter/material.dart';

class BidDetailScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Bid Detail Screen"),
      ),
      body: Center(
        child: Text("Bid Detail Screen"),
      ),
    );
  }
}
